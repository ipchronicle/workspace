# IPChronicle Workspace Instructions

## 语言与协作

- 默认使用简体中文回复用户。
- 直接、务实、简洁，先给结论，再提供必要依据。
- 对分析、讨论和审查请求不要擅自修改文件。
- 对明确的实现任务，先检查相关仓库，再制定短计划、实现和验证。

## 开始工作前

必须先阅读 `docs/project-background.md`。该文件是理解项目起因、已确认决策和未决事项的入口。

不要从仓库名称、空目录或旧项目代码推断技术栈和最终架构。当前 GitHub 上的 `server`、`web`、`agent`、`deploy` 仓库只是预留位置，可以在架构讨论后合并、改名、停用或替换。

## Workspace 职责

workspace 仓库只维护：

- 项目背景和跨仓库上下文
- 已确认的架构决策记录
- 对多个仓库都适用的协作规则
- 在确有重复需求后建立的开发辅助设施

workspace 不承载产品源码，也不预先规定构建工具、开发环境、仓库清单或版本锁定方式。

## 旧项目边界

`references/komari-ip-history` 是旧项目的只读参考检出。它用于了解历史需求、用户流程、踩坑记录和已有验证场景，不是新项目的基础代码。

除非用户明确确认，否则不要：

- 复制旧项目架构或大段实现
- 保持旧 API、数据库或部署兼容
- 继续以 Komari 作为核心依赖
- 默认保留旧项目的功能和技术选型

需要借鉴旧实现时，先说明借鉴对象、理由和替代方案。

## 多仓库工作

- 修改前分别检查 workspace 和相关子仓库的 Git 状态。
- 阅读距离目标文件最近的 `AGENTS.md`。
- 每个仓库保持独立提交，不从 workspace 根仓库提交嵌套仓库内容。
- 每完成一个可独立验证的功能点，就在受影响的仓库中创建对应的本地提交；不要把已经完成的多个功能长期积压在工作区。跨仓库功能按仓库分别提交，并保持提交目的对应。
- 不使用 Git submodule，除非后续明确决定采用。
- 不假设所有预留仓库都必须参与某项功能。
- 跨仓库长期约束应记录到 `docs/decisions/`。

## 工程与安全

- 修复根因，不添加第二套事实来源、静默回退或伪成功路径。
- 不提交真实凭据、环境文件、本地数据库、依赖目录、构建产物或 VM 状态。
- 不回退无关的用户改动，不使用破坏性 Git 操作。
- 新增大型依赖、服务或基础设施前，先解释为什么更简单的方案不够。
- 日常开发和本地预览迭代只运行与改动风险匹配的定向测试；不要为每个小改动重复执行完整发布级检查。
- 执行任何 `git push` 前，必须运行相关产品仓库定义的全量检查，并按改动范围补充浏览器、Compose 或发布检查。全量检查未通过时不得推送；无法运行时先说明原因并等待用户决定。
- 检查 diff 中的秘密、生成物和无关修改。

## 发布前数据兼容

- 当前项目尚未发布任何已投入线上使用的版本。除非用户以后明确说明已经发布并存在需要保留的线上数据，否则所有开发、测试和本地预览产生的持久化数据均不承诺跨版本兼容。
- 在此阶段不要为配置库、历史库、Agent 状态或其他持久化格式编写升级迁移、数据转换、双读双写、旧格式回退或兼容分支。直接修改现有 schema、迁移基线或存储格式，并在需要时重建本地开发数据。
- 本地开发数据不能读取时，应明确报错或执行干净重建，不要为了保留未发布数据增加产品复杂度。
- 只有用户明确说明某个版本已经发布并投入线上使用后，才从该版本的数据格式开始评估迁移和兼容要求；不得从版本号、Git 标签、候选发布名称、镜像或仓库状态自行推断已经上线。

## 文档

文档必须脱离聊天上下文仍可独立理解。技术栈、功能范围、协议、存储、部署和仓库边界等长期决定，应在用户确认后写入决策记录，不要把尚未决定的方案写成既定事实。
