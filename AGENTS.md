# IPChronicle Workspace Instructions

## 语言与协作

- 默认使用简体中文回复用户。
- 直接、务实、简洁，先给结论，再提供必要依据。
- 对分析、讨论和审查请求不要擅自修改文件。
- 对明确的实现任务，先检查相关仓库，再制定短计划、实现和验证。

## 开始工作前

必须先阅读 `docs/project-background.md`。该文件是理解项目起因、已确认决策和未决事项的入口。

不要从历史占位仓库、空目录或旧项目代码推断技术栈和最终架构。产品与仓库边界以已接受的架构决策和当前产品仓库为准。

## Workspace 职责

workspace 仓库只维护：

- 项目背景和跨仓库上下文
- 已确认的架构决策记录
- 对多个仓库都适用的协作规则
- 在确有重复需求后建立的开发辅助设施

workspace 不承载产品源码，也不预先规定构建工具、开发环境、仓库清单或版本锁定方式。

## 旧项目边界

`references/komari-ip-history` 是旧项目的只读参考检出。它用于了解历史需求、用户流程、踩坑记录和已有验证场景，不是新项目的基础代码。

除非用户明确确认，否则不要：

- 复制旧项目架构或大段实现
- 保持旧 API、数据库或部署兼容
- 继续以 Komari 作为核心依赖
- 默认保留旧项目的功能和技术选型

需要借鉴旧实现时，先说明借鉴对象、理由和替代方案。

## 多仓库工作

- 修改前分别检查 workspace 和相关子仓库的 Git 状态。
- 阅读距离目标文件最近的 `AGENTS.md`。
- 每个仓库保持独立提交，不从 workspace 根仓库提交嵌套仓库内容。
- 每完成一个可独立验证的功能点，就在受影响的仓库中创建对应的本地提交；不要把已经完成的多个功能长期积压在工作区。跨仓库功能按仓库分别提交，并保持提交目的对应。
- 不使用 Git submodule，除非后续明确决定采用。
- 不按运行时组件拆分产品仓库，除非新的长期边界已经通过 ADR 确认。
- 跨仓库长期约束应记录到 `docs/decisions/`。

## 工程与安全

- 修复根因，不添加第二套事实来源、静默回退或伪成功路径。
- 不提交真实凭据、环境文件、本地数据库、依赖目录、构建产物或 VM 状态。
- 不回退无关的用户改动，不使用破坏性 Git 操作。
- 新增大型依赖、服务或基础设施前，先解释为什么更简单的方案不够。
- 日常开发和本地预览迭代只运行与改动风险匹配的定向测试；不要为每个小改动重复执行完整发布级检查。
- 执行任何 `git push` 前，必须运行相关产品仓库定义的本地 preflight。竞态、完整构建、双架构镜像、浏览器、Compose、发行版矩阵、资源限制、可复现构建和发布失败门禁等高资源检查由 GitHub Actions 执行；当前提交的普通 CI 未全部通过前，不得创建候选产物、标签或发布版本。需要定位失败时再在本地运行对应的重型检查。
- 检查 diff 中的秘密、生成物和无关修改。

## 稳定版数据兼容

- `v0.1.0` 已于 2026-09-03 作为首个受支持稳定版发布并投入线上使用，其配置库、历史库和 Agent 本地状态构成初始兼容基线。
- 后续同一主版本的存储变更必须保留从 `v0.1.0` 及所有仍受支持稳定版本向前升级的路径。Center 使用新的有序迁移，Agent 变更遵守状态升级与回滚边界；不得重写已经发布的迁移基线。
- 涉及持久格式的改动必须增加相应的旧版本夹具或发布产物驱动的升级测试，并验证配置、历史、身份、凭据引用和离线队列等相关不变量。
- `v0.1.0` 之前的开发版本和 RC 数据不属于受支持升级来源，不要为其增加迁移、双读双写、旧格式回退或兼容分支。
- 不兼容或损坏的数据必须明确报错。不得静默删除、重建、替换或重新解释稳定版数据。
- 具体迁移和兼容要求遵循 ADR 0023、0025、0026、0058 和 0068。

## 文档

文档必须脱离聊天上下文仍可独立理解。技术栈、功能范围、协议、存储、部署和仓库边界等长期决定，应在用户确认后写入决策记录，不要把尚未决定的方案写成既定事实。

- 面向用户和贡献者的公开文档默认使用简体中文；需要英文版本时使用同目录、同名的 `.en.md` 文件，并在两个版本顶部提供双向语言链接。
- 修改双语文档时必须同步维护中文和英文内容。许可证、第三方声明以及其他要求保留法定原文的材料不受此规则约束。
